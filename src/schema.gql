# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

type Account {
  createdBy: User!
  createdDate: String!
  id: ID!
  name: String!
  permissions: [AccountPermission!]!
  users: [User!]!
}

type AccountPermission {
  id: ID!
  name: String!
}

input AddAccountInput {
  name: ID!
  permissionIds: [String!]
  usersIds: [String!]
}

input AddAccountPermissionInput {
  name: String!
}

input AddUserAccountInput {
  accountId: String!
  userId: String!
}

type Mutation {
  addAccount(account: AddAccountInput!): Account!
  addAccountPermissions(accountPermission: AddAccountPermissionInput!): AccountPermission!
  addUserAccount(userAccountInput: AddUserAccountInput!): UserAccount!
  newRequestSubmission(requestSubmission: RequestSubmissionInput!): RequestSubmission!
}

type Query {
  account(accountId: String!): Account!
  requestSubmissions: [RequestSubmission!]!
  user(userId: String!): User!
}

"""Status of the request"""
enum RequestStatus {
  CLOSED
  HANDLED
  OPEN
}

type RequestSubmission {
  case: String!
  createdDate: String!
  email: String!
  id: ID!
  name: String!
  phoneNumber: String!
  status: RequestStatus!
}

input RequestSubmissionInput {
  case: String!
  email: String!
  name: String!
  phoneNumber: String!
}

type User {
  accounts: [Account!]!
  id: ID!
  name: String!
  permissions: [UserPermission!]!
}

type UserAccount {
  accountId: String!
  userId: String!
}

type UserPermission {
  id: ID!
  name: String!
}
