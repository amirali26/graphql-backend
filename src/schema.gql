# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

type Account {
  areasOfPractices: [AreasOfPractice!]!
  createdBy: User!
  createdDate: String!
  id: ID!
  name: String!
  permissions: [AccountPermission!]!
  users: [User!]!
}

type AccountPermission {
  id: ID!
  name: String!
}

input AddAccountInput {
  areasOfPracticeIds: [String!]!
  name: ID!
  permissionIds: [String!]
  receiveEmails: Boolean!
  usersIds: [String!]
}

input AddAccountPermissionInput {
  name: String!
}

input AddUserAccountInput {
  accountId: String!
  userId: String!
}

type AreasOfPractice {
  id: ID!
  name: String!
}

type Mutation {
  addAccount(account: AddAccountInput!): Account!
  addAccountPermissions(accountPermission: AddAccountPermissionInput!): AccountPermission!
  addUserAccount(userAccountInput: AddUserAccountInput!): UserAccount!
}

type Query {
  account(accountId: String!): Account!
  areasOfLegalPractices: [AreasOfPractice!]!
  requestSubmissions: [RequestSubmission!]!
  user(userId: String!): User!
}

"""Status of the request"""
enum RequestStatus {
  CLOSED
  HANDLED
  OPEN
}

type RequestSubmission {
  case: String!
  createdDate: String!
  email: String!
  id: ID!
  name: String!
  phoneNumber: String!
  status: RequestStatus!
  topic: String!
}

type User {
  accounts: [Account!]!
  birthDate: String!
  email: String!
  id: ID!
  name: String!
  permissions: [UserPermission!]!
  phoneNumber: String!
}

type UserAccount {
  accountId: String!
  userId: String!
}

type UserPermission {
  id: ID!
  name: String!
}
